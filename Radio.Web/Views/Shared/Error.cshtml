@{
    ViewData["Title"] = "خطأ";
    var error = ViewData["ErrorMessage"] as string;
}

<div class="flex items-center justify-center min-h-[60vh] p-4">
    <div class="card text-center p-10 max-w-md w-full">
        <div class="w-16 h-16 bg-danger-subtle rounded-full flex items-center justify-center mx-auto mb-5">
            <span class="material-symbols-rounded text-3xl text-danger" aria-hidden="true">error</span>
        </div>
        <h1 class="text-headline font-bold text-text mb-2">حدث خطأ</h1>
        <p class="text-body text-text-muted mb-6">@(error ?? "حدث خطأ غير متوقع أثناء معالجة طلبك. يرجى المحاولة مرة أخرى لاحقاً.")</p>
        <div class="flex items-center justify-center gap-3">
            <a href="/" class="btn btn-primary">
                <span class="material-symbols-rounded text-lg" aria-hidden="true">home</span>
                الرئيسية
            </a>
            <button onclick="history.back()" class="btn btn-outline">
                <span class="material-symbols-rounded text-lg" aria-hidden="true">arrow_back</span>
                رجوع
            </button>
        </div>
    </div>
</div>

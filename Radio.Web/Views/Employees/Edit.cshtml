@model EmployeeDto
@{
    ViewData["Title"] = Model.EmployeeId == 0 ? "موظف جديد" : "تعديل موظف";
    var isEdit = Model.EmployeeId != 0;
    var staffRoles = ViewBag.StaffRoles as List<StaffRoleDto>;
}

<div class="mb-6">
    <nav class="text-sm text-ink-muted mb-2">
        <a href="/Employees" class="hover:text-primary">الموظفون</a>
        <span class="mx-1">/</span>
        <span class="text-ink">@(isEdit ? "تعديل" : "جديد")</span>
    </nav>
    <h1 class="text-2xl font-bold text-ink flex items-center gap-2">
        <span class="material-symbols-rounded text-primary">@(isEdit ? "edit" : "add")</span>
        @(isEdit ? "تعديل موظف" : "موظف جديد")
    </h1>
</div>

<form method="post" action="@(isEdit ? $"/Employees/Edit/{Model.EmployeeId}" : "/Employees/Create")" class="max-w-2xl">
    @Html.AntiForgeryToken()
    @if (isEdit) { <input type="hidden" name="EmployeeId" value="@Model.EmployeeId" /> }
    @if (ViewContext.ModelState.ErrorCount > 0)
    {
        <div asp-validation-summary="All" class="bg-red-50 border border-red-200 text-danger rounded-lg p-3 mb-4"></div>
    }

    <div class="bg-surface rounded-xl border border-border p-5 space-y-4">
        <div>
            <label class="form-label">الاسم الكامل *</label>
            <input type="text" name="FullName" value="@Model.FullName" class="form-input" required />
        </div>
        <div>
            <label class="form-label">الدور الوظيفي *</label>
            <select name="StaffRoleId" class="form-input" required>
                <option value="">— اختر —</option>
                @foreach (var r in staffRoles ?? new())
                {
                    <option value="@r.StaffRoleId" selected="@(Model.StaffRoleId == r.StaffRoleId)">@r.RoleName</option>
                }
            </select>
        </div>
        <div>
            <label class="form-label">ملاحظات</label>
            <textarea name="Notes" rows="3" class="form-input">@Model.Notes</textarea>
        </div>
    </div>

    <div class="flex items-center justify-end gap-2 mt-4">
        <a href="/Employees" class="btn-secondary"><span class="material-symbols-rounded">cancel</span> إلغاء</a>
        <button type="submit" class="btn-primary"><span class="material-symbols-rounded">save</span> حفظ</button>
    </div>
</form>
